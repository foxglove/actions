name: PR Review

on:
  pull_request: {}

jobs:
  review:
    permissions:
      actions: read
      contents: read
      pull-requests: write
      id-token: write
    uses: ./.github/workflows/review.yml
    secrets:
      ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
